<%= form_for(@voucher_particular) do |f| %>
  <% if @voucher_particular.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voucher_particular.errors.count, "error") %> prohibited this voucher_particular from being saved:</h2>

      <ul>
      <% @voucher_particular.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
